<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/tts_bottom_sheet"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="?attr/colorSurface"
    android:elevation="8dp"
    android:padding="16dp"
    app:behavior_hideable="true"
    app:behavior_peekHeight="0dp"
    app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

    <ProgressBar
        android:id="@+id/tts_progress_bar"
        style="@style/Widget.MaterialComponents.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:progress="0"
        android:max="100"
        android:progressTint="@color/tts_playing" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingTop="16dp"
        android:paddingBottom="8dp">

        <ImageButton
            android:id="@+id/btn_tts_previous"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@android:drawable/ic_media_previous"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/previous_chapter"
            android:tint="?attr/colorOnSurface" />

        <ImageButton
            android:id="@+id/btn_tts_play_pause"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@android:drawable/ic_media_play"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/play_pause"
            android:tint="?attr/colorOnSurface"
            android:layout_marginHorizontal="16dp" />

        <ImageButton
            android:id="@+id/btn_tts_next"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@android:drawable/ic_media_next"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/next_chapter"
            android:tint="?attr/colorOnSurface" />

        <Space
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/tv_tts_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tts_stopped"
            android:textSize="16sp"
            android:textColor="?attr/colorOnSurface"
            android:layout_gravity="center_vertical" />

        <ImageButton
            android:id="@+id/btn_tts_settings"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@android:drawable/ic_menu_preferences"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/tts_settings"
            android:tint="?attr/colorOnSurface"
            android:layout_marginStart="16dp" />

        <ImageButton
            android:id="@+id/btn_tts_stop"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/ic_stop"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/stop"
            android:tint="?attr/colorOnSurface"
            android:layout_marginStart="8dp" />

    </LinearLayout>

    <TextView
        android:id="@+id/tv_tts_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/tts_ready"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end" />

</LinearLayout>
